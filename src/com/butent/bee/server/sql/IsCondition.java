package com.butent.bee.server.sql;

/**
 * Extends <code>IsSql, HasSource</code> interfaces, is the interface for condition building
 * classes.
 */

public interface IsCondition extends IsSql, HasSource {
}
