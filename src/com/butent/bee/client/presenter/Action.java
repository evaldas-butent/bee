package com.butent.bee.client.presenter;

/**
 * Lists possible actions with a user interface component (like refresh, save, close etc).
 */

public enum Action {
  ADD, BOOKMARK, CLOSE, CONFIGURE, DELETE, REFRESH, SAVE

}
