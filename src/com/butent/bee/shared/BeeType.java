package com.butent.bee.shared;

/**
 * Contains a list of possible data and variable types.
 */

public enum BeeType {
  BOOLEAN, STRING, CHAR, TEXT,
  NUMBER, BYTE, SHORT, INT, LONG, FLOAT, DOUBLE,
  DATE, ENUM, NULL
}
